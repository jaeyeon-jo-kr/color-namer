{erl_opts, [debug_info]}.

{deps, [{clojerl, "0.7.1"}, lager, {cowboy, "2.6.3"}]}.
{plugins, [rebar3_clojerl]}.

{relx, [ { release
         , {event_server, "0.1.0"}
         , [ sasl, event_server
           ]
         },

        {sys_config, "./config/sys.config"},
        {vm_args, "./config/vm.args"},

        {dev_mode, true},
        {include_erts, false},

        {extended_start_script, true}]
}.

{ profiles
, [ { prod
    , [ {relx, [ {dev_mode, false}
               , {include_erts, true}
               ]
        }
      ]
    }
  ]
}.
